var documenterSearchIndex = {"docs":
[{"location":"benchmarks/#Benchmarks","page":"Benchmarks","title":"Benchmarks","text":"By avoiding global memory as much as reasonably possible we are much faster than implementations using high-level convolution calls. The Julia package SSIMLoss.jl falls under this last category, as does the Python package pytorch-msssim. On the other hand fused-ssim is a Python package (well, CUDA + other backends, with PyTorch bindings) with a similar approach to ours. Below we will compare the (CUDA) performance of these packages, as well as ImageQualityIndexes.jl as a CPU-only method for reference. \n\nWe should point out that fused_ssim.py uses zero-padding like us, while SSIMLoss.jl uses either valid padding (i.e. cropping) or symmetric padding. pytorch_msssim uses valid padding. Writing c for the number of color channels in an image, h for its width and w for its height, ImageQualityIndexes.jl and FastCUDASSIM.jl use images in (c, h, w) format, while SSIMLoss.jl uses (h, w, c), and the Python packages use (w, h, c) when converted to column-major order.\n\nThe benchmarking code can be found under benchmark in the GitHub repo. The results on a system equipped with an intel i7-7700K CPU and RTX 3070 GPU are shown below. We always present the median execution time. \n\nSSIM for a single full HD RGB image pair:\n\n    * ImageQualityIndexes.jl:                 638     ms\n    * SSIMLoss.jl (`crop = false`):            60.3   ms\n    * pytorch_msssim.py:                        8.35  ms\n    * fused_ssim.py:                            0.875 ms\n    * FastCUDASSIM.jl:                          0.988 ms\n\nDSSIMs and gradients for a batch of 32 grayscale images of size 256 x 256:\n\n    * SSIMLoss.jl (Zygote):                     ERROR: Gradient Thunk(ChainRules.var\"#...) should be a tuple\n    * pytorch_msssim.py:                        12.0   ms\n    * fused_ssim.py:                             0.765 ms\n    * FastCUDASSIM.jl (Zygote):                  1.10  ms\n    * FastCUDASSIM.jl (`dssim_with_gradient!`):  0.824 ms\n\nIn these benchmarks we used\n\nJulia 1.12.5\nCUDA 5.9.7 (runtime 13.1, driver 591.59.0, compiler 13.2)\nImageQualityIndexes 0.3.7\nSSIMLoss 1.0.0\nFastCUDASSIM 1.0.0-DEV\nPython 3.10.20\nPyTorch 2.7.1+cu118\npytorch_msssim 1.0.0 (latest commit: b057b07, August 16, 2023)\nfused_ssim 1.0.0 (latest commit: a7c48d6, February 2, 2026).","category":"section"},{"location":"formats/#Input-and-output-formats","page":"Input and output formats","title":"Input and output formats","text":"We assume that image pixel intensities are normalized between 0 and 1. This is not checked in the code, i.e. fully the responsibility of the caller.","category":"section"},{"location":"formats/#sizes","page":"Input and output formats","title":"sizes","text":"Internally we always use input and output image batches with channels x height x width x batch size layout, which we will refer to as canonical format. However, this does not mean we require a CuArray{T, 4}, only that our input needs to be able to be converted to it using reshapes adding singleton dimensions, and channelview. For example, a CuMatrix{RGB{N0f8}] of size (256, 384) is equivalent to a CuArray{N0f8, 3} of size (3, 256, 384), which in turn we can reshape to include the batch axis, resulting in a CuArray{N0f8, 4} with size (3, 256, 384, 1). Such conversions are taken care of automatically.\n\njulia> using FastCUDASSIM, CUDA, ImageCore\n\njulia> img1 = CUDA.rand(RGB{N0f8}, 256, 384); img2 = CUDA.rand(RGB{N0f8}, 256, 384);\n\njulia> ssim(img1, img2)\n0.015220874f0\n\nThis is mostly equivalent to\n\njulia> ssim(reshape(channelview(img1), 3, size(img1)..., 1), reshape(channelview(img2), 3, size(img2)..., 1))\n1-element CuArray{Float32, 1, CUDA.DeviceMemory}:\n 0.015220872\n\nWe will come back to the difference in output format.\n\nIn these size conversions we first add a channel axis if necessary, then a batch axis. This implies that non-Colorant images with three dimensions will be considered as a single color image, to which we need to add a singleton batch dimension. That is, an image of eltype (say) Float32 and size (x, y, z) will be interpreted as an x-channel image with height y and width z, equivalent to a singleton batch of size (x, y, z, 1). In particular, we do not consider it as a grayscale batch of z images of size (x, y), which would have canonical size (1, x, y, z). It is still possible to provide such a grayscale batch of course, either by explicitly providing it in the latter format, or by using Gray{Float32} as eltype, and size (x, y, z).\n\nIn the code example above the only difference was in the output format. The idea here is that we will try to make the output similar to the first input image. In the first example we had not provided a batch axis, and therefore the output does not have one either. I.e. we just return a scalar. In the second example the input to ssim did have a batch axis, and therefore so does the produced output ssim value CuVector. The same conversion principle applies to the output gradients.\n\njulia> img1 = CUDA.rand(Float32, 256, 384); img2 = CUDA.rand(Float32, 256, 384);\n\njulia> ssim_gradient(img1, img2)\n256×384 CuArray{Float32, 2, CUDA.DeviceMemory}:\n  9.98839f-6 ...\n  ...        ...\n\njulia> ssim_gradient(reshape(img1, 1, size(img1)...), reshape(img2, 1, size(img2)...))\n1×256×384 CuArray{Float32, 3, CUDA.DeviceMemory}:\n[:, :, 1] =\n 9.98839f-6 ...\n ...        ...\n\njulia> ssim_gradient(reshape(img1, 1, size(img1)..., 1), reshape(img2, 1, size(img2)..., 1))\n1×256×384×1 CuArray{Float32, 4, CUDA.DeviceMemory}:\n[:, :, 1, 1] =\n 9.98839f-6 ...\n ...        ...\n\nIn all these examples the size of the returned gradient is the same as that of the first input argument. While we advise against it, it is possible to use different formats for the two input images, as they will be canonicalized seperately. But for the output format we solely rely on the first image.\n\njulia> ssim_gradient(reshape(img1, 1, size(img1)..., 1), img2)\n1×256×384×1 CuArray{Float32, 4, CUDA.DeviceMemory}:\n...\n\nMaking the output similar to the input includes matching Colorants if relevant:\n\njulia> img1 = CUDA.rand(RGB{Float32}, 256, 384); img2 = CUDA.rand(RGB{Float32}, 256, 384);\n\njulia> ssim_gradient(img1, img2)\n256×384 CuArray{RGB{Float32}, 2, CUDA.DeviceMemory}:\n RGB(3.75159f-6, -3.50066f-6, -3.8947f-6) ...\n ...                                      ...\n\nFor the in-place methods we will again perform size conversions automatically, whenever reasonably possible. An important remark here is that the internal buffers N_dssims_dQ and friends use canonical height x channels x width x batch size memory order (for memory coalescing). Therefore, they cannot be provided as e.g. a CuMatrix{RGB{Float32}} as this uses channels x height x width order. We also do not allow Gray inputs here. Additionally, in e.g. ssim! we always expect a CuVector of length the (possibly implicit) batch size, never a scalar.\n\nThe following example is purely illustrative. Again, to avoid confusion we advise to use as consistent input formats as possible.\n\njulia> img1 = CUDA.rand(256, 384); img2 = CUDA.rand(1, 256, 384, 1);  # Both (1, 256, 384, 1) in canonical format\n\njulia> ssim_vector = CuArray{Float32}(undef, 1)               # Must be a CuVector\n       dL_dimg1 = CuArray{Float32}(undef, 1, 256, 384);       # Will be converted to canonical size (1, 256, 384, 1)\n       N_dssim_dQ = CuArray{Float32}(undef, 256, 384);        #                                     (256, 1, 384, 1)\n       N_dssim_dM = CuArray{Float32}(undef, 256, 1, 384);     #                                     (256, 1, 384, 1)\n       N_dssim_dP = CuArray{Float32}(undef, 256, 1, 384, 1);  # Already in canonical format\n    \njulia> CUDA.@sync ssim_with_gradient!(ssim_vector, dL_dimg1, img1, img2, N_dssim_dQ, N_dssim_dM, N_dssim_dP)  # Returns ssim_vector and dL_dimg1 (in their input format)\n(Float32[0.019175043], Float32[-1.0864862f-5 ...;;; ...])","category":"section"},{"location":"formats/#eltypes","page":"Input and output formats","title":"eltypes","text":"Our Gaussian SSIM kernel is hard-coded as Float32. When it is used in the convolutions, the output will be of the promotion type of the eltype of the input image with Float32. When an allocating method is used, we will then use the promotion type of both images' eltypes and Float32 for the output. For example,\n\njulia> img1 = CUDA.rand(Float16, 32, 48); img2 = similar(img1);\n\njulia> ssim(img1, img2)\n5.400778f-6\n\njulia> promote_type(eltype(img1), eltype(img2), Float32)\nFloat32\n\njulia> ssim(img1, Float64.(img2))\n5.400778328318361e-6\n\njulia> promote_type(eltype(img1), Float64, Float32)\nFloat64\n\nFor N0f8 and N0f16 the promotion type is also Float32.\n\njulia> ssim_with_gradient(cu(N0f16.(Array(img1))), cu(Gray{N0f8}.(Array(img2))))\n(5.40085f-6, Float32[-3.435738f-8 ...])\n\nIn this example we first of all point out that the CuMatrix{Gray{N0f8}} is internally converted to a CuArray{N0f8, 4}, where we added the singleton gray color channel and batch axes. Then for the output we have promote_type(N0f16, N0f8, Float32) === Float32. Secondly, we need to pass through the CPU to construct the Normed inputs. The reason for this is that the Normed constructor has a check possibly throwing an error with a string-interpolated message. This is not supported on the device.\n\nFor in-place methods the convolutions will still take place as described before, but in the final stage when directly preparing the values to write away, we will work with the provided type. Normed eltypes are not supported, as explained above.\n\njulia> CUDA.@sync ssim!(CUDA.zeros(Float16, 1), img1, img2, false)\n1-element CuArray{Float16, 1, CUDA.DeviceMemory}:\n 5.4e-6\n\nOf course, we would advise against using too low a precision. We primarily wrote the code with Float32 in mind and do not guarantee sufficient numerical stabitility for lower precision.","category":"section"},{"location":"formats/#nothing","page":"Input and output formats","title":"nothing","text":"Finally, we mention that for in-place methods like ssim_with_gradient! you can use nothing for one of the outputs or for the intermediate gradient buffers. In this case we will internally allocate the required arrays with appropriate eltypes (the same ones as would be used for the allocating methods like ssim_with_gradient). Buffers provided as nothing will be automatically unsafe_free!d. The same is true for the singleton ssims CuVector if we do not return it, but its only scalar value.\n\njulia> img1 = CUDA.rand(Float32, 32, 48); img2 = similar(img1); dL_dimg1 = similar(img1)\n\njulia> CUDA.@sync ssim_with_gradient!(nothing, dL_dimg1, img1, img2, nothing, nothing, nothing)\n(5.8797564f-6, Float32[-2.607109f-8 ...])\n\njulia> CUDA.@sync ssim_with_gradient!(nothing, dL_dimg1, reshape(img1, (1, size(img1)..., 1)), img2, nothing, nothing, nothing)\n(Float32[5.8797564f-6], Float32[-2.607109f-8 ...])\n\nIn both cases we internally allocate a singleton CuVector for the single SSIM value. In the first case we extract its value and free the CuVector, while in the second case we return (and obviously do not free) the CuVector.","category":"section"},{"location":"reference/#Reference","page":"Reference","title":"Reference","text":"","category":"section"},{"location":"reference/#Index","page":"Reference","title":"Index","text":"","category":"section"},{"location":"reference/#Reference-2","page":"Reference","title":"Reference","text":"","category":"section"},{"location":"reference/#FastCUDASSIM.FastCUDASSIM","page":"Reference","title":"FastCUDASSIM.FastCUDASSIM","text":"Package for computing the Structural Similarity Index Measure (SSIM) and its gradients on NVIDIA GPUs.\n\nQuick start\n\njulia> using FastCUDASSIM, CUDA\n\njulia> img1 = CUDA.rand(Float32, 3, 512, 768); img2 = similar(img1);\n\njulia> ssim(img1, img2)  # Actual value depends on RNG above\n4.7778763f-6\n\nKey points\n\nCUDA only\nWe use zero-padding in the convolutions. The convolution kernel is the typical Gaussian with standard deviation sigma = 15 and window size 11 x 11. We represent it using Float32.\nWe expect Float-like image intensities in [0, 1]. This includes N0f8.\nInternally we use channels x height x width x batch size format for the input image batches, and height x channels x width x batch size for internal buffers (N_dssims_dQ and friends). Supplied inputs need not have this explicit size, but must have an equivalent memory layout.\n\nImage formats\n\nTo expand on the last point, consider a CuMatrix{RGB{N0f8}} of size (256, 384). It is equivalent (via channelview) to a CuArray{N0f8, 3} of size (3, 256, 384), and (via a further reshape) to a CuArray{N0f8, 4} with size (3, 256, 384, 1). We will perform these conversions automatically. Therefore, this input is supported.\nIn these conversions we will (if necessary) first expand the channels dimension, followed by the batch dimension. Therefore, a CuArray{Float32} of size (x, y, z) will be considered to be a single x-channel image of height y and width z. To input a grayscale batch consisting of z images of size (x, y), either use a CuArray{Gray{Float32}, 3} with size (x, y, z), or explicitly set the number of color channels to 1 via a CuArray{Float32, 4} of size (1, x, y, z).\nWhen we internally allocate an output (D)SSIM or gradient batch (either because an allocating method like dssim_with_gradient is called, or because in one of the mutating methods like dssim_with_gradient! the supplied value was nothing), we will convert this back to a format similar to that of the first input image (batch). For example, if no batch axis appears here, then we will return a scalar (D)SSIM value instead of a CuVector of length 1. There might be some promotions, though. For example, ssim_gradient(imgs1, imgs2) for imgs1 a CuArray{RGB{N0f8}, 3} of size (256, 384, 16) and imgs2 similar, will return a CuArray{RGB{Float32}, 3} gradient batch, again of size (256, 384, 16).\n\n\n\n\n\n","category":"module"},{"location":"reference/#FastCUDASSIM.dssim","page":"Reference","title":"FastCUDASSIM.dssim","text":"dssim(\n    imgs1, imgs2, \n    divide_by_two_in_dssim = false\n)\n\nCompute the Structural DisSimilarity Index Measure (DSSIM) values between two image batches.\n\nLike dssim_with_gradient, but without the gradient.\n\nSee also ssim, and dssim! for the non-allocating version of dssim. \n\n\n\n\n\n","category":"function"},{"location":"reference/#FastCUDASSIM.dssim!","page":"Reference","title":"FastCUDASSIM.dssim!","text":"dssim!(\n    dssims, \n    imgs1, imgs2, \n    should_zero = true,\n    divide_by_two_in_dssim = false\n)\n-> dssims\n\nCompute the DSSIMs between two image batches.\n\nLike ssim!, but for the structural dissimilarity DSSIM = 1 - SSIM if  !divide_by_two_in_dssim (the default) or DSSIM = (1 - SSIM) / 2 otherwise. \n\nSee also dssim_with_gradient!.\n\n\n\n\n\n","category":"function"},{"location":"reference/#FastCUDASSIM.dssim_gradient","page":"Reference","title":"FastCUDASSIM.dssim_gradient","text":"dssim_gradient(\n    imgs1, imgs2, \n    divide_by_two_in_dssim = false\n)\n\nCompute the gradients of the DSSIMs between two image batches with respect to the first. \n\nLike dssim_with_gradient, but does not compute the DSSIM itself.\n\nSimilar to ssim_gradient, but for the DSSIM = 1 - SSIM when  !divide_by_two_in_dssim (the default), or DSSIM = (1 - SSIM) / 2 otherwise. This method allocates (and frees) a number of buffers and of course also allocates the  output gradient 'image' batch. See dssim_gradient! for the non-allocating version.\n\n\n\n\n\n","category":"function"},{"location":"reference/#FastCUDASSIM.dssim_gradient!","page":"Reference","title":"FastCUDASSIM.dssim_gradient!","text":"dssim_gradient!(\n    dL_dimgs1, \n    imgs1, imgs2,\n    N_dssims_dQ, N_dssims_dM, N_dssims_dP,\n    divide_by_two_in_dssim = false\n)\n-> dL_dimgs1\n\nCompute the gradients of the DSSIMs between two image batches with respect to the first one.\n\nLike dssim_with_gradient!, but without the computation of the DSSIM itself, and like ssim_gradient!, but for the DSSIM = 1 - SSIM when !divide_by_two_in_dssim (the default) or DSSIM = (1 - SSIM) / 2 otherwise.\n\n\n\n\n\n","category":"function"},{"location":"reference/#FastCUDASSIM.dssim_with_gradient","page":"Reference","title":"FastCUDASSIM.dssim_with_gradient","text":"dssim_with_gradient(\n    imgs1, imgs2, \n    divide_by_two_in_dssim = false\n)\n\nCompute the DSSIMs between two image batches, as well as their gradients with respect to the first one.\n\nLike ssim_with_gradient, but for the structural dissimilarity DSSIM = 1 - SSIM when !divide_by_two_in_dssim (the default), or DSSIM = (1 - SSIM) / 2 otherwise. See also dssim_with_gradient! for the non-allocating version.\n\n\n\n\n\n","category":"function"},{"location":"reference/#FastCUDASSIM.dssim_with_gradient!","page":"Reference","title":"FastCUDASSIM.dssim_with_gradient!","text":"dssim_with_gradient!(\n    dssims, dL_dimgs1, \n    imgs1, imgs2,\n    N_dssims_dQ, N_dssims_dM, N_dssims_dP,\n    should_zero = true,\n    divide_by_two_in_dssim = true\n)\n-> (dssims, dL_dimgs1)\n\nCompute the DSSIMs between two image batches, as well as the gradients with respect to the first one.\n\nLike ssim_with_gradient!, but for the structural dissimilarity DSSIM = 1 - SSIM when !divide_by_two_in_dssim (by default), or DSSIM = (1 - SSIM) / 2.\n\n\n\n\n\n","category":"function"},{"location":"reference/#FastCUDASSIM.ssim!","page":"Reference","title":"FastCUDASSIM.ssim!","text":"ssim!(\n    ssims, \n    imgs1, imgs2, \n    should_zero = true\n)\n-> ssims\n\nCompute the SSIMs between two image batches.\n\nLike ssim_with_gradient! but does not compute the gradient. See this function for the meaning of the arguments.\n\n\n\n\n\n","category":"function"},{"location":"reference/#FastCUDASSIM.ssim-Tuple{Any, Any}","page":"Reference","title":"FastCUDASSIM.ssim","text":"ssim(imgs1, imgs2)\n\nCompute the Structural Similarity Index Measure (SSIM) values between two image batches.\n\nLike ssim_with_gradient, but without the gradient.\n\nSee the module documentation for the input and output formats. Also see ssim! for the non-allocating version.\n\n\n\n\n\n","category":"method"},{"location":"reference/#FastCUDASSIM.ssim_gradient!-NTuple{6, Any}","page":"Reference","title":"FastCUDASSIM.ssim_gradient!","text":"ssim_gradient!(\n    dL_dimgs1, \n    imgs1, imgs2,\n    N_dssims_dQ, N_dssims_dM, N_dssims_dP,\n)\n-> dL_dimgs1\n\nCompute the gradients of the SSIM between two image batches with respect to the first one.\n\nLike ssim_with_gradient!, but without the computation of the SSIM itself.\n\n\n\n\n\n","category":"method"},{"location":"reference/#FastCUDASSIM.ssim_gradient-Tuple{Any, Any}","page":"Reference","title":"FastCUDASSIM.ssim_gradient","text":"ssim_gradient(imgs1, imgs2)\n\nCompute the gradients of the SSIMs between two image batches with respect to the first. \n\nLike ssim_with_gradient, but does not compute the SSIM itself.\n\nThis method allocates (and frees) a number of buffers and of course also allocates the  output gradient 'image' batch. See ssim_gradient! for the non-allocating version.\n\n\n\n\n\n","category":"method"},{"location":"reference/#FastCUDASSIM.ssim_with_gradient!","page":"Reference","title":"FastCUDASSIM.ssim_with_gradient!","text":"ssim_with_gradient!(\n    ssims, dL_dimgs1, \n    imgs1, imgs2,\n    N_dssims_dQ, N_dssims_dM, N_dssims_dP,\n    should_zero = true\n)\n-> (ssims, dL_dimgs1)\n\nCompute the SSIMs between two image batches, as well as the gradients with respect to the first one.\n\nSee the module documentation for the input and output formats.\n\nArguments\n\nssims: The (output) CuVector which will contain the SSIM values.\ndL_dimgs1: The (output) gradients of imgs1 with respect to the SSIM value L.\nimgs1: The first image batch, with respect to which we compute the gradients.\nimgs2: The second image batch.\nN_dssims_dQ, N_dssims_dM and  N_dssims_dP: Internal buffers for intermediate   gradients.\nshould_zero: To get the correct SSIM value we need ssims to start with values of 0.   When set to true we will perform this zeroing inside the method. Otherwise it is the   responsibility of the caller to have done this in advance.   Defaults to true.\n\nReturns\n\n(ssims, dL_dimgs1)\n\n\n\n\n\n","category":"function"},{"location":"reference/#FastCUDASSIM.ssim_with_gradient-Tuple{Any, Any}","page":"Reference","title":"FastCUDASSIM.ssim_with_gradient","text":"ssim_with_gradient(imgs1, imgs2)\n\nCompute the SSIMs between two image batches, as well as the gradients with respect to the first one.\n\nThis method allocates (and frees) a number of buffers and of course also allocates the  output gradient 'image' batch. See ssim_with_gradient! for the non-allocating version.\n\nSee the module documentation for expected formats for the inputs and output.\n\nArguments\n\nimgs1: The first image batch.\nimgs2: The second image batch.\n\nReturns\n\nThe SSIM values.\nThe gradients of these values with respect to imgs1.\n\n\n\n\n\n","category":"method"},{"location":"math/#Mathematics","page":"Mathematics","title":"Mathematics","text":"The following equations and notation can help in understanding the code, especially for the backward pass.","category":"section"},{"location":"math/#Forward","page":"Mathematics","title":"Forward","text":"Let matrK in mathbbR^(2r + 1) times (2r + 1) be the Gaussian SSIM kernel with radius r in mathbbN (normally r = 5): \n\nK_ji propto exp left(-fraci^2 + j^22sigma^2 right)\n\nwhere sigma = 15, we indexed matrK via -r leq i j leq r, and the proportionality constant is such that sum_ji K_ji = 1. For any grayscale image matrI in mathbbR^h times w, the convolution matrM coloneqq matrM_matrI coloneqq matrI * matrK in mathbbR^h times w contains local means (locally averaged intensities) from matrI, i.e. local first moments. Note that we will use zero-padding to make matrM the same size as matrI. Similarly, matrQ = matrI^2 * matrK (where the square is elementwise) contains local second (quadratic) moments. Local variances can then be computed as matrV = matrQ - matrM^2, cf. VarX = EX^2 - EX^2 for a random variable X. If we have a second (reference) image matrR in mathbbR^h times w and matrP = (matrI odot matrR) * matrK with odot denoting the Hadamard (i.e. componentwise) product, then matrC = matrP - matrM_matrI odot matrM_matrR contains local covariances between the images, cf. CovX Y = EXY - EXEY.\n\nThe SSIM map matrS(matrI matrR) in mathbbR^h times w between our images is now defined as\n\nmatrS(matrI matrR) = frac(2 matrM_matrI odot matrM_matrR + alpha)(2matrC + beta)(matrM_matrI^2 + matrM_matrR^2 + alpha)(matrV_matrI + matrV_matrR + beta)\n\nHere all operations are elementwise, and alpha and beta are two small constants to avoid numerical instability by division by (near) zero. If the intensities are normalised to lie in 0 1, we will use alpha = 001^2 and beta = 003^2, following standard practices. The actual SSIM is then simply the mean of the SSIM map. If our images have multiple color channels, we take the average of the per-channel SSIM values.","category":"section"},{"location":"math/#In-the-code","page":"Mathematics","title":"In the code","text":"We always work with batches of images with one or more color channels. Because the batch axis is the last one, the separate images are contiguous in memory, so we can simply loop over them. Therefore, we will only consider a single image pair from now on. The channel axis is the first one, so we cannot do the same for the color channels. Instead we will always process all channels of a pixel at the same time. Because the operations are the same for each channel and we simply average in the end, we will again only consider grayscale images.\n\nWe will exploit that the kernel matrK is separable, so that we can replace a 2D convolution with matrK by two 1D convolutions, one horizontal and one vertical. This (single) 1D kernel vectk in mathbbR^2r + 1 satisfies\n\nk_i propto exp left(-fraci^22sigma^2 right)\n\nfor -r leq i leq r. Also note that all kernels are symmetric, so that there is no difference between convolution and cross-correlation:\n\n(matrI * matrK)_ji = sum_q p I_j-q i-p K_q p = sum_q p I_j+q i+p K_-q -p = sum_q p I_j+q i+p K_q p = (matrI star matrK)_ji\n\nWe want to avoid materialising the matrices we mentioned above (e.g. matrM_matrI) as much as possible. Therefore, we only implicitly store them, in shared memory. We divide the SSIM map to compute (as well as the input images) into non-overlapping tiles. Each of these tiles will be handled by a single thread-block, where each pixel in a tile will be computed by one thread in the block. If a thread needs to find the SSIM map at location (j i), it needs access to the values in the square between (j - r i - r) and (j + r i + r). Therefore, each thread-block will load its own pixels from matrI and matrR into shared memory, but with a margin of r pixels to all sides. Consequently we might go out of bounds for matrI and matrR, in which case we implicitly pad using zeros. So if our blocks are of size H times W we need to load an enlarged tile of size (H + 2r) times (W + 2r). The horizontal convolution then reduces this to (H + 2r) times W and the vertical convolution to H times W. Note that the different (H + 2r) times (W + 2r) tiles do overlap, and that for pixels in overlap regions we will compute convolutions multiple times. But this is worth it to avoid having to go to global memory.\n\nAt the end we will have obtained the SSIM map in local memory. Since we are only interested in the SSIM map itself, we only need the average. We can then immediately reduce the SSIM map blockwise. Reducing it further to a single value is trickier, as this requires inter-block memory accesses. We found atomic operations to work efficiently in this situation.","category":"section"},{"location":"math/#Backward","page":"Mathematics","title":"Backward","text":"Writing L = SSIM(matrI matrR) in mathbbR, we want to find fracpartial Lpartial matrI in mathbbR^1 times (h times w). ","category":"section"},{"location":"math/#Computational-graph","page":"Mathematics","title":"Computational graph","text":"Our mathematical computations in the forward pass can be visualized in the following computational graph.\n\n(Image: Computational graph for the SSIM)\n\nThe variables here are\n\nmatrI in mathbbR^h times w: the (grayscale) input image with respect to which we want to find the gradient  \nmatrR in mathbbR^h times w: the reference image (we consider constant)\nmatrM in mathbbR^h times w: matrM = matrI * matrK the first moments (means)\nmatrK in mathbbR^(2r + 1) times (2r + 1) is the Gaussian SSIM kernel of radius r in mathbbN\nmatrJ in mathbbR^h times w: matrJ = matrI odot matrI, the componentwise square of matrI\nmatrQ in mathbbR^h times w: matrQ = matrJ * matrK: the quadratic moments\nmatrH in mathbbR^h times w: matrH = matrI odot matrR: the Hadamard product of matrI and matrR\nmatrP in mathbbR^h times w: matrP = matrH * matrK: the averaged product, to be used for the covariance.\n\nAfter this phase where we have to work across different pixels due to the convolution, all pixels are handled independently, so we just fix one pixel. The value of the matrices (in bold capital letters) at this pixel are written in lower case. E.g. q is one value in the matrQ-matrix. The following variables are then\n\nv = q - m^2: the local variance in image matrI in the neighbourhood centered around our pixel\nc = p - m cdot m_R: the local covariance between images matrI and matrR\nm_R is the matrK-weighted mean of matrR at our pixel, i.e. an entry in matrM_matrR = matrR * matrK. Like matrR itself, it is a constant for the purposes of obtaining the gradient with respect to matrI.\nx = 2 m cdot m_R + alpha: the first numerator factor in the SSIM map formula\nalpha = 001^2 is the first stabilization constant for the division\ny = 2 c + beta: the second numerator factor in the SSIM map formula\nbeta = 003^2 is the second stabilization constant\nz = m^2 + m_R^2 + alpha: the first denominator factor\nt = v + v_matrR + beta: the second denominator factor\nwith v_matrR the matrR-equivalent to matrI's v\ns = fracxyzt: the SSIM map value at our pixel.\n\nFinally, the SSIM L is the average of all the N SSIM map values s: L = frac1N sum_ji S_ji.","category":"section"},{"location":"math/#Gradients","page":"Mathematics","title":"Gradients","text":"Most of the gradients are trivial to compute:\n\nfracpartial Lpartial s = frac1N, where N is the number of pixels in the image.\nfracpartial Lpartial x = fracpartial Lpartial sfracpartial spartial x = frac1N fracyzt (=fracsNx)\nfracpartial Lpartial y = frac1N fracxzt (=fracsNy)\nfracpartial Lpartial z = -frac1N fracxyz^2t (=-fracsNz)\nfracpartial Lpartial t = -frac1N fracxyzt^2 (=-fracsNt)\nfracpartial Lpartial v = fracpartial Lpartial t fracpartial tpartial v = -frac1N fracxyzt^2 (=-fracsNt)\nfracpartial Lpartial c = fracpartial Lpartial y fracpartial ypartial c = frac2N fracxzt (=frac2sNy)\nfracpartial Lpartial q = fracpartial Lpartial v fracpartial vpartial q = -frac1N fracxyzt^2 (=-fracsNt)\nfracpartial Lpartial p = fracpartial Lpartial c fracpartial cpartial p = frac2N fracxzt (=frac2sNy)\nfracpartial Lpartial m = fracpartial Lpartial x fracpartial xpartial m + fracpartial Lpartial z fracpartial zpartial m + fracpartial Lpartial v fracpartial vpartial m + fracpartial Lpartial c fracpartial cpartial m = frac1N fracyzt cdot 2m_matrR - frac1N fracxyz^2t cdot 2m -frac1N fracxyzt^2 cdot (-2m) + frac2N fracxzt cdot (-m_matrR) = frac2 (y - x)Nzt m_matrR + frac2xyNztleft( frac1t - frac1z right) m = frac2Nzt left( (y - x) m_matrR + fracxyzt( z - t) m right) = frac2Nzt left( (y - x) m_matrR + s( z - t) m right)\n(Alternatively: fracpartial Lpartial m = frac2sNx m_matrR - frac2sNz m + frac2sNt m - frac2sNy m_matrR = frac2sN left( left(frac1x - frac1y right) m_matrR + left(frac1t - frac1z right) m right))\n\nThese then form the components of the gradient 'images' fracpartial Lpartial matrQ fracpartial Lpartial matrP fracpartial Lpartial matrM in mathbbR^1 times (h times w) cong mathbbR^h times w. Now note that for e.g. fracpartial Lpartial matrJ = fracpartial Lpartial matrQ fracpartial matrQpartial matrJ in mathbbR^1 times (h times w) we have\n\nleft(fracpartial Lpartial matrJright)_1ji = sum_lk left(fracpartial Lpartial matrQright)_1lk left(fracpartial matrQpartial matrJright)_lkji = sum_lk fracpartial Lpartial Q_lk fracpartial Q_lkpartial J_ji\n\nRecalling that matrQ = matrJ * matrK, i.e. Q_lk = sum_q p = -r^r J_l-q k-p K_qp, we see that fracpartial Q_lkpartial J_ji = K_l-jk-i when -r leq (q =) l - j (p =) k - i leq r, and 0 otherwise. Therefore,\n\nleft(fracpartial Lpartial matrJright)_1ji = sum_qp fracpartial Lpartial Q_j+qi+p K_qp\n\nIn other words, fracpartial Lpartial matrJ = fracpartial Lpartial matrQ star matrK. Note that we need to use zero-padding here. For example, the top-left pixel in matrJ affects (r + 1)^2 entries in matrQ = matrJ * matrK, namely the in-bounds ones from the surrounding square of side length 2r + 1. There are no out of bounds contributions, so those entries in fracpartial Lpartial matrQ need to be considered as 0.\n\nWe can now find the final gradients:\n\nfracpartial Lpartial matrJ = fracpartial Lpartial matrQ star matrK\nleft(fracpartial Lpartial matrIright)_matrJ = fracpartial Lpartial matrJ odot 2 matrI\nThe subscript matrJ indicates that in the computational graph we are only considering the path going through matrJ.\nfracpartial Lpartial matrH = fracpartial Lpartial matrP star matrK\nleft(fracpartial Lpartial matrIright)_matrH = fracpartial Lpartial matrH odot matrR\nleft(fracpartial Lpartial matrIright)_matrM = fracpartial Lpartial matrM star matrK\nfracpartial Lpartial matrI = left(fracpartial Lpartial matrIright)_matrJ + left(fracpartial Lpartial matrIright)_matrH + left(fracpartial Lpartial matrIright)_matrM.\n\nFor color images not much changes as we deal with the channels separately. Instead of fixing a pixel, we fix a pixel component, and N now is the total number of pixel components.","category":"section"},{"location":"math/#In-the-code-2","page":"Mathematics","title":"In the code","text":"In the forward pass we already compute and save fracpartial Lpartial matrQ, fracpartial Lpartial matrM and fracpartial Lpartial matrP. (Note that if we are only interested in the gradient and not the SSIM value itself, we do not even have to perform the reduction on the implicit local memory SSIM map matrS.) The backward pass then only corresponds to computing and combining the cross-correlations of these matrices with matrK. We will use separate kernels for the forward and backward pass, and really store fracpartial Lpartial matrM and friends in global memory. The alternative where we keep everything in shared memory would require working with overlapping tiles of (H + 4r) times (W + 4r) pixels to eventually produce after two 2D cross-correlations a gradient block of H times W. The amount of redundant computations outweights the benefit of not going to global memory.","category":"section"},{"location":"math/#A-note-on-padding","page":"Mathematics","title":"A note on padding","text":"We could have allowed convolutions to decrease the size of the images (valid padding). For example, when matrI in mathbbR^h times w, then matrM = matrI * matrK in mathbbR^(h - 2r) times (w - 2r). For the SSIM value we then ignore the boundary pixels of matrI and matrR. Python's skimage does this for example (though by cropping the full SSIM map in mathbbR^h times w), and so does and SSIMLoss.jl by default (crop = true).\n\nIn the backward pass, however, we still need zero-padding: to get fracpartial Lpartial matrJ in mathbbR^h times w as fracpartial Lpartial matrJ = fracpartial Lpartial matrQ star matrK, we would need to pad fracpartial Lpartial matrQ in mathbbR^(h - 2r) times (w - 2r) to mathbbR^(h + 2r) times (w + 2r), because the top-left pixel of matrJ contributes to precisely one pixel: the top-left pixel in the smaller fracpartial Lpartial matrQ. In particular, we cannot again allow the cross-correlation to reduce the size (to mathbbR^(h - 4r) times (w - 4r)).\n\nOn the other hand, if we use zero-padding in the forward pass, matrJ's top-left pixel receives more information: from the entire top-left length-(r + 1) square of fracpartial Lpartial matrQ in mathbbR^h times w. This will likely be advantageous during training.","category":"section"},{"location":"#FastCUDASSIM.jl","page":"Home","title":"FastCUDASSIM.jl","text":"Fast calculation of the Structural Similarity Index Measure (SSIM) and its gradients, on NVIDIA GPUs.","category":"section"},{"location":"#Quick-start","page":"Home","title":"Quick start","text":"julia> using Pkg; Pkg.add(\"CUDA\"); Pkg.add(url = \"https://github.com/LaurensDiels/FastCUDASSIM.jl\")  # Currently not yet registered\n\njulia> using FastCUDASSIM, CUDA\n\njulia> img1 = CUDA.rand(Float32, 3, 512, 768); img2 = similar(img1);\n\njulia> ssim(img1, img2)  # Actual value depends on RNG above\n4.7802605f-6\n\njulia> ssim_gradient(img1, img2)  # w.r.t. img1\n3×512×768 CuArray{Float32, 3, CUDA.DeviceMemory}:\n[:, :, 1] =\n -4.05717f-11 ...\n ...          ...\n\njulia> using Zygote\n\njulia> Zygote.gradient(x -> ssim(x, img2), img1)\n(Float32[-4.057169f-11 ...],)\n\njulia> using TestImages\n\njulia> dssim(cu(testimage(\"cameraman.tif\")), cu(testimage(\"mandril_gray.tif\")))  # CuMatrix{Gray{N0f8}} inputs\n0.8373802f0","category":"section"},{"location":"#Key-points","page":"Home","title":"Key points","text":"Fast by avoiding global memory as much as reasonably possible. See the benchmarks.\nSupport for CUDA only: no other graphics APIs, no CPU fallback.\nReverse-mode autodiff integration via ChainRules.jl. Works out of the box with Zygote.jl.\nThe convolutions use zero padding and the usual Gaussian kernel with (conceptual) window size 11 times 11 and sigma = 15, in Float32 precision.\nImage intensities are assumed to lie in 0 1.\nImages should use [channels x] height x width [[x batch size]] memory layout. See Input and output formats for more details.","category":"section"},{"location":"#Contents","page":"Home","title":"Contents","text":"Pages = (p -> p.second).(Main.PAGE_NAMES_AND_FILES)","category":"section"},{"location":"autodiff/#Automatic-differentiation","page":"Automatic differentiation","title":"Automatic differentiation","text":"We include ChainRulesCore.jl rrules for ssim and dssim as a package extension. Consequently automatic differentiation using Zygote.jl is supported out of the box. Note that like in ssim_gradient we only differentiate with respect to the first image (batch). The same conventions about the input and output format apply.\n\nAn example:\n\njulia> using FastCUDASSIM, CUDA, Zygote, ImageCore\n\njulia> img1 = CUDA.rand(Gray{N0f8}, 2, 3); img2 = CUDA.rand(Gray{N0f8}, 2, 3);\n\njulia> Zygote.withgradient(ssim, img1, img2)\n(val = 0.6785328f0, grad = (Gray{Float32}[0.0048529073 0.38347715 0.36758304; 0.20223245 0.5144576 0.11673692], nothing))\n\nIn particular, the gradient with respect to img2 is nothing. Also note that for such tiny images our SSIM value is quite high due to the zero-padding.\n\nWhen using dssim as (part of) a training loss, ensure the reference image is the second input.\n\njulia> render = CUDA.rand(Float32, 2, 3); ground_truth = CUDA.rand(Float32, 2, 3);\n\njulia> Zygote.withgradient(y -> dssim(y, ground_truth), render)\n(val = 0.101208925f0, grad = (Float32[-0.26408646 -0.19994365 -0.011348972; 0.029529918 -0.077309705 0.029900908],))\n\n\n\nAlthough less convenient and flexible, when possible consider using the in-place functions like dssim_with_gradient! to squeeze out every bit of performance (cf. the benchmarks).\n\njulia> render_batch = CUDA.rand(Float32, 3, 32, 48, 128)  # Batch of 128 3-channel images of height 32 and width 48 pixels\n\njulia> ground_truth_batch = similar(render_batch);\n\njulia> dL_render_batch = similar(render_batch);\n       dssims = CuArray{Float32}(undef, 128);             # batch size\n       N_dssims_dQ = CUDA.rand(Float32, 32, 3, 48, 128);  # height x channels x width x batch size\n       N_dssims_dM = similar(N_dssims_dQ);\n       N_dssims_dP = similar(N_dssims_dQ);\n\njulia> dssim_with_gradient!(dssims, dL_render_batch, render_batch, ground_truth_batch, N_dssims_dQ, N_dssims_dM, N_dssims_dP);\n\njulia> # (...): Use dL_render_batch to update the parameters of the model outputting render_batch (in-place)\n\njulia> dssim_with_gradient!(dssims, dL_render_batch, render_batch, ground_truth_batch, N_dssims_dQ, N_dssims_dM, N_dssims_dP);","category":"section"},{"location":"precompilation/#Precompilation","page":"Precompilation","title":"Precompilation","text":"We have an extensive precompilation suite which should cover most common argument types for all our methods. However, running it takes quite some time. Additionally, the CUDA kernels themselves are currently not yet persistently stored[1] so that the effect of precompilation is only limited (though certainly noticeable). For this reason we use Preferences.jl to also allow for partial precompilation, or none at all. We provide three options:\n\n\"off\": Do not precompile anything from FastCUDASSIM.jl itself.\n\"fast\" (the default): Precompile all methods for image batches with 1 or 3 color channels and eltype Float32. Also precompile ssim and dssim for Gray{N0f8} and RGB{N0f8}.\n\"full\": The full suite.\n\nThe desired option can be selected via\n\nusing Preferences\nset_preferences!(\"FastCUDASSIM\", \"precompilation\" => <option name>)\n\nwithin your environment. If the LocalPreferences.toml file already exists with a section [FastCUDASSIM] containing precompilation, you can use the option force = true to overwrite the value (see also below).\n\nThe effects of the different options are shown below, on a system with an intel i7-7700K CPU and NVIDIA RTX 3070 GPU. Between each run we manually deleted the .julia/compiled/v1.x/FastCUDASSIM.jl directory, though this is not necessary: after a set_preferences! call and after reloading FastCUDASSIM.jl, precompilation will again be triggered. This includes when going down from e.g. \"full\" to \"fast\".\n\noff:\n\njulia> using Preferences, CUDA\n\njulia> set_preferences!(\"FastCUDASSIM\", \"precompilation\" => \"off\")\n\njulia> using FastCUDASSIM\nPrecompiling FastCUDASSIM finished.\n  1 dependency successfully precompiled in 19 seconds. 116 already precompiled.\n\njulia> x = CUDA.rand(Float32, 128, 256); y = similar(x);\n\njulia> @time ssim_with_gradient(x, y);\n 16.066590 seconds (30.73 M allocations: 1.472 GiB, 1.65% gc time, 182.96% compilation time: 1% of which was recompilation)\n\njulia> @time ssim_with_gradient(x, y);\n  0.003356 seconds (8.91 k allocations: 455.367 KiB, 1416.90% compilation time)\n\nfast:\n\njulia> using Preferences, CUDA\n\njulia> set_preferences!(\"FastCUDASSIM\", \"precompilation\" => \"fast\"; force = true)\n\njulia> using FastCUDASSIM\nPrecompiling FastCUDASSIM finished.\n  1 dependency successfully precompiled in 67 seconds. 116 already precompiled.\n\njulia> x = CUDA.rand(Float32, 128, 256); y = similar(x);  # covered by the \"fast\" precompilation\n\njulia> @time ssim_with_gradient(x, y);  # better than with \"off\", but not great\n 11.367837 seconds (22.07 M allocations: 1.054 GiB, 1.10% gc time, 183.18% compilation time: 2% of which was recompilation)\n\njulia> @time ssim_with_gradient(x, y);\n  0.005376 seconds (12.07 k allocations: 619.776 KiB, 1 lock conflict, 586.16% compilation time)\n\nfull:\n\njulia> using Preferences, CUDA\n\njulia> set_preferences!(\"FastCUDASSIM\", \"precompilation\" => \"full\"; force = true)\n\njulia> using FastCUDASSIM\nPrecompiling FastCUDASSIM finished.\n  1 dependency successfully precompiled in 281 seconds. 116 already precompiled.\n\njulia> x = CUDA.rand(Float32, 128, 256); y = similar(x);\n\njulia> @time ssim_with_gradient(x, y);\n 11.501233 seconds (20.55 M allocations: 1005.402 MiB, 2.08% gc time, 180.51% compilation time: 5% of which was recompilation)\n\njulia> @time ssim_with_gradient(x, y);\n  0.002357 seconds (8.51 k allocations: 431.929 KiB, 1051.01% compilation time)\n\n[1]: See https://discourse.julialang.org/t/how-to-precompile-cuda-kernel-itself/121849/ and the relevant GitHub issues.","category":"section"}]
}
